name: fl_build
description: Build script for Flutter proj.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.4.0

dependencies:
  # path: ^1.8.0

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
